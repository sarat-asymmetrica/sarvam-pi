# Experiment 004: Asymmetrica Runtime Smoke

Purpose: expose one narrow, read-only Asymmetrica Runtime capability after provider and tool-loop behavior are stable.

Candidate first tools:

- health check
- catalog/list kernels
- harmless classifier call
