# Experiment 003: RLM State Smoke

Purpose: externalize context and history so compaction does not erase task continuity.

The first pass should persist JSONL trajectories and separate compaction summaries.
